USE SCHEMA SourceVault;

CREATE TABLE INVOICE (
    INVOICENO INTEGER,
    DEBTORID INTEGER,
    INVOICEDATE DATE,
    INVOICESTATUS VARCHAR,
    JUICEERPSALESORDERID INTEGER,
        CONSTRAINT pk_INVOICE PRIMARY KEY (INVOICENO)
);
