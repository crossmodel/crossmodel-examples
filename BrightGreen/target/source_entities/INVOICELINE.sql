USE DATABASE CrossModel;
USE SCHEMA SourceVault;

CREATE TABLE INVOICELINE (
    INVOICENO INT,
    INVOICELINENO INT,
    LINEDESCRIPTION VARCHAR,
    PRICE DECIMAL,
    VAT DECIMAL,
    QUANTITY INT,
        CONSTRAINT pk_INVOICELINE PRIMARY KEY (INVOICENO, INVOICELINENO)
);
