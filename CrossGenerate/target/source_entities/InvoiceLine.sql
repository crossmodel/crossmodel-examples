CREATE TABLE SourceVault.INVOICELINE (
    INVOICENO INT,
    INVOICELINENO INT,
    LINEDESCRIPTION VARCHAR,
    PRICE DECIMAL,
    VAT DECIMAL,
    QUANTITY INT,
        CONSTRAINT pk_INVOICELINE PRIMARY KEY (INVOICENO, INVOICELINENO)
);
GO