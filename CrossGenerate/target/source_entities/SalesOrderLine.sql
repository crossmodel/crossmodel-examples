CREATE TABLE SourceVault.SalesOrderLine (
    SalesOrderID INT,
    ProductID INT,
    ItemPrice DECIMAL(, ),
    Quantity INT,
    LineAmt DECIMAL(, ),
        CONSTRAINT pk_SalesOrderLine PRIMARY KEY (SalesOrderID)
);
GO